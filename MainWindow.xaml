<Window x:Class="DropFiles.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DropFiles"
        mc:Ignorable="d"
        Title="文件中转站" Height="450" Width="400"
        AllowDrop="True"
        PreviewDragEnter="Window_PreviewDragEnter"
        Drop="Window_Drop"
        DragLeave="Window_DragLeave"
        Icon="icon.png">
    
    <Window.Resources>
        <!-- 定义文件项的数据模板 -->
        <DataTemplate x:Key="FileItemTemplate">
            <StackPanel Orientation="Horizontal" Margin="2">
                <Image Source="{Binding Path=Icon}" Width="16" Height="16" />
                <TextBlock Text="{Binding Path=FileName}" Margin="5,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>

        <!-- 定义 ListBoxItem 的样式 -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightGray"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="DodgerBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <!-- 主内容区域 -->
        <Grid>
            <ListBox
                Name="FileList"
                SelectionMode="Extended"
                ItemTemplate="{StaticResource FileItemTemplate}"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch" 
                AllowDrop="True"
                PreviewMouseLeftButtonDown="FileList_PreviewMouseLeftButtonDown"
                KeyDown="FileList_KeyDown">
            </ListBox> 
        </Grid>

        <!-- 拖放提示覆盖层 -->
        <Grid Name="DropHintOverlay" Visibility="Collapsed" Background="#80000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBlock Text="拖放到此处" FontSize="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
